import React from "react";
import ReactDOM from "react-dom/client";
import MultiCheckbox from "./App.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MultiCheckbox />
  </React.StrictMode>
);
